apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "www.moratilla.com.fullname" . }}-test-connection"
  labels:
    {{- include "www.moratilla.com.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "www.moratilla.com.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
